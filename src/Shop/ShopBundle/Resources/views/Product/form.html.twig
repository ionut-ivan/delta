{#
<form action="{{path ('shop_shop_productpage', {'catid' : product.categoryId.Id , 'id': product.id})}}" method="post" {{ form_enctype(form) }}>  
      {{ form_widget(form) }}
        <br>
        <button type="submit" title="Add to Cart" class="button btn-cart" ><span><span>Add to Cart</span></span></button>
       
</form>
#}